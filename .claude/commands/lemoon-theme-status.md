Show a combined status of the Shopify themes and local git state.

Run both of these and present the results clearly:

1. `shopify theme list` — show all themes, highlighting which is live and which is the dev theme
2. `git status` — show local uncommitted changes
3. `git log --oneline -5` — show the 5 most recent commits

Summarize: what's changed locally that hasn't been pushed to either theme.
